The object detection code runs on another pickled version of the dataset, 
which has 'x' as the input images and 'y' as the name of the object.Due to large size of that dataset,
it has not been included. 
